# Copy to repo root as `.env` for local backend (never commit `.env`)
APP_ENV=development
HTTP_ADDR=:8080

DATABASE_URL=postgres://ilo:ilo_dev_password@localhost:5432/ilo?sslmode=disable
REDIS_URL=redis://localhost:6379/0

# Change this before any shared/staging deploy
JWT_SECRET=dev-only-change-me-ilo-super-secret-key
JWT_TTL_HOURS=72
