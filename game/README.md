# Game (Unreal Engine 5)

Folder ini untuk project **ILO** (Indonesia Life Online).

## Status

| Item | Status |
|---|---|
| Unreal project (`.uproject`) | **Belum** — tunggu UE5 terpasang di mesin |
| Setup guide | [`docs/UE_SETUP.md`](../docs/UE_SETUP.md) |
| Integrasi backend | [`docs/UE_INTEGRATION.md`](../docs/UE_INTEGRATION.md) |

## Setelah UE terpasang

Buat project **Third Person** bernama `ILO` **di dalam folder ini** (`game/`), lalu commit `.uproject` + `Config/` + `Content/` (yang perlu) + `Source/` bila C++.

Jangan commit build artifacts (lihat `.gitignore` root).
